#include "main.h"
/**
 * _isupper - checks upper case
 * @c: print int
 * Return: give 1 if c is uppercase or 0 otherwise
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}

else
{
return (0);
}

}
